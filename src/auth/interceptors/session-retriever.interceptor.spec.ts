import { SessionRetrieverInterceptor } from './session-retriever.interceptor';

describe('SessionRetrieverInterceptor', () => {
  it('should be defined', () => {
    expect(new SessionRetrieverInterceptor()).toBeDefined();
  });
});
